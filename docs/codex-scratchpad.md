# Codex scratchpad: remaining work after PR #1 review

This is the handoff and remaining-work ledger for Codex. Implementation resumed with explicit owner authorization after the review landed. This records integrated changes, active isolated work and remaining requirements; worker reports alone are not completion claims.

## Current database stack — board acceptance and remaining review work

The swappable-database stack is active. PostgreSQL and MySQL board flows now have native and pinned-image evidence, but final combined acceptance and remaining review changes are not complete. Cross-engine transfer remains unimplemented. Preserve the owner’s design/review files and distinguish prepared worker changes from integrated code.

| Layer | Current evidence |
| --- | --- |
| PRs #2–4 | Previously pushed review fixes remain; consult the [review checklist](stack-review-status.md) for unresolved items rather than treating green checks as review resolution. |
| [PR #7](https://github.com/CryogenicPlanet/artifactory/pull/7) | `6f86e49`: all checks green. New ledger/shape review changes remain isolated. |
| [PR #8](https://github.com/CryogenicPlanet/artifactory/pull/8) | `24cc343`: all checks green. New isolation/read/JSON review changes remain isolated. |
| [PR #9](https://github.com/CryogenicPlanet/artifactory/pull/9) | Pushed `709ae2c`: both actual PostgreSQL/MySQL board CI jobs, remote tests, QEMU and image green; Linux shards pending. Local anchor-only correction `a9c9e33` has an exact-case rerun running, not yet accepted. |

The full runtime suite at exact `709ae2c` completed with actual Node 22.22.3 and at most two workers: **1,019 passed, one failed and 59 skipped (1,079 total)**; **231 files passed, one failed and 24 skipped (256 total)**, in **655.69s** (`/tmp/comms-runtime-full-709ae2c.log`). The failure is stale preparation-phase instrumentation after the validated-launch refactor. `a9c9e33` corrects its anchor; the rerun is still pending at this checkpoint. Do not call this a green combined suite or infer test counts from check/build log suffixes.

Native PostgreSQL 18.6 completed auth, writes/replay, backup, restore, restart and subsequent writes at `795c738`. Native MySQL 8.4.11 completed its full flow at `048332f`, including interrupted rollback, restore/restart, a second backup and verified closure. Those native versions remain separate from pinned PostgreSQL 17.11/MySQL 8.4.11 image evidence. The second-backup check proves creation and ownership cleanup, not an independent restore of that second artifact.

Portable protected-migration checks (`aae09da`) are integrated with 25-case focused evidence. Stream caps, exact MySQL privilege revocation/closure corrections and bounded operation diagnostics are integrated in the tested `709ae2c` production tree (based on `e41cfe6`). These are retained production boundaries, not acceptance of the new review fixes below. The MySQL session-metadata correction also remains: account-filtered `session_account_connect_attrs` is world-readable on MySQL 8.4; redundant grants were removed after 2/2 native ACL tests. Separate provisioning and boot-only XA privileges are still required.

New reviews [#7](https://github.com/CryogenicPlanet/artifactory/pull/7#issuecomment-5644472625) and [#8](https://github.com/CryogenicPlanet/artifactory/pull/8#issuecomment-5644467131) have now been read completely. Active isolated work covers ledger authority/diagnostics/derived targets; independent historical boot fixtures and shape checks; lower-layer editable-receipt protection (`579be1e`); MySQL isolation assertion; lower snapshot readers, dialect semantics and published JSON casts; and runtime core JSON migration/codecs. **None of those prepared fixes is accepted as integrated here.** The [checklist](stack-review-status.md) records each finding and distinguishes runtime-only fixes from lower-PR obligations.

Transfer storage/value foundations are prepared, unintegrated: five codec and seven SQLite tests pass, native execution is unrun. There is no transfer CLI, board copy or completion/source-retirement marker workflow yet. Remote SQL repair remains bounded to 200 returned rows and a 1,000-row/1 MiB protected-write image budget; advanced stored-object rehearsal and broader direct-DDL policy remain unanswered decisions. Finish those decisions, remaining review fixes, and final combined acceptance before claiming the whole swappable-database goal complete.

## Base checkpoint before database resumption — fourth review

The owner’s current direction is **boot ownership scope, not a line-count target**. [The boot ownership audit](boot-ownership-audit.md) records the controlling decisions and where they intentionally narrow older requirements. The user’s stated boundary assigns application event browsing to the editable app; the subsequent direct-boot request-log delta is recorded below. **Retain source version inspection and safe revert**, including durable outcomes and preserved messages, pages and identities. The second-pass review remains relevant subject to these explicit scope decisions.

**Earlier base-first checkpoint (superseded by the database resumption above):** PRs #2–4 and prepared migration/remote-client work were paused. Owner commit `5c20fea` records the [third-pass review](pr-1/third-pass-2ee17ac.md) and [numbered review decisions](pr-1/pr-comments.md): 32 assigns application event browsing to the app, 33 removes anchored editing while retaining conditional writes and source history, and 34 removes boot metrics/child-span aggregation. Owner SPEC, tech and PR-review documents are not edited by this ledger.

The pre-merge documentation checkpoint was `0b63a74`, including committed owner UI work `7bfb24a` and the revised root README. These later changes have not received a combined full-suite run. The [fourth review](pr-1/fourth-pass-efa6de5.md), recorded by owner commit `7181a8b`, opens items 44–50 in the [review ledger](pr-1/pr-comments.md). Initial triage confirms the missing readiness dispatch (49), the legacy probe running after schema migration (45), and repair admission problems (46); fixes are not yet integrated. The [user-facing documentation pass](README.md) is complete: 29 active Markdown documents reviewed, all active links resolve, and 32 shell/JavaScript/TypeScript snippets pass syntax checks. Fresh combined documentation review is clear. All 21 owner SPEC/tech/database/review-document hashes and two historical audits remain unchanged (`/tmp/comms-docs-pass-owner-hashes.json`). This pass made no runtime changes or full-suite claim; concurrent owner auth/pages changes are outside its scope.

The last local full acceptance is tests `efa6de5` against production `ee32d13`: **775 passed, one skipped (776 total), 194 passed files and one skipped file, 553.73s**, with actual Node 22.22.3 and two workers. [Linux 34661713424](https://github.com/CryogenicPlanet/artifactory/actions/runs/34661713424) subsequently finished **773 passed, two timeout failures and one skipped**; image and [QEMU 34661714216](https://github.com/CryogenicPlanet/artifactory/actions/runs/34661714216) passed. Reviewed test-only timeout corrections `b1814f7` and `303e6fb` remain isolated, not integrated. Neither the local pass nor those isolated corrections accept the current combined branch.

The ownership direction comes from the user’s conversation: scope matters, not line count; boot’s public event surface should describe boot’s own events, application browsing belongs to editable code, and source history/safe revert must remain. Calendar retirement and legacy coordinator removal are implemented agent changes; do not represent items 35/36 as separately approved decisions. The fourth review explicitly asks for confirmation of calendar/privacy consequences and further repair of legacy refusal. Preserve the owner’s SPEC, tech, database and review documents; this ledger reports implementation and evidence rather than rewriting those decisions.

Implemented changes: `e38d60c` retires calendar pruning/day settings while preserving exact old signed-setting receipts; `ca5eb0a` removes those UI controls. `8cc783e` replaces legacy topic-move replay with a table-presence refusal and disables pruning on that unsupported-store path. `31bd792` refreshes initial volume measurements before admission; `9aaf4e1` removes extra public-page caps while retaining the shared 1 MiB transport bound.

`98e5df9` retries accepted-cutover lock finalization/intent deletion while preserving healthy PID, epoch, newer writes and borrowed staging. Mandatory PUT compare-and-set is integrated: `baseVersion` accepts a hash or `null`, with header aliases; missing PUT conditions return 400 and stale content returns 409. DELETE conditions remain optional. Root recovery/settings passes 18/18 across four files in 18.69s. Separate calendar/legacy/volume/public-path/cleanup groups pass 9/4/8/8/12; conditional-write groups pass 140 with one skip across 41 files. These overlapping focused groups are not a combined full-suite count.

The suite changed from 808 to 776 cases as retired calendar/legacy-coordinator tests were removed or replaced and conditional-write/accepted-cleanup cases were added. The final pass is not a rerun of an identical baseline case set.

### Historical verification before final acceptance

These results and CI status snapshots apply only to their named checkpoints.

The `ee32d13` full run finished 769 passed, six failed and one skipped across 193 passed, one failed and one skipped file in 569.09s (`/tmp/comms-final-base-full-ee32d13.log`). All failures were in recovery-intent fixtures: three expected the old legacy error and three omitted mandatory PUT conditions. Test-only `efa6de5` retains store/source/evidence assertions and passes 9/9 focused cases in 9.79s before the full pass above. Superseded [ee32 Linux 34661043905](https://github.com/CryogenicPlanet/artifactory/actions/runs/34661043905) and [QEMU 34661043903](https://github.com/CryogenicPlanet/artifactory/actions/runs/34661043903) are historical links, not final-head acceptance.

The prior `5ff231e` full pass below predates this completion wave. Later `1859d87` Linux finished 806 passed, one failed and one skipped; a fixture SQLITE_BUSY failure was corrected with a bounded busy timeout. Superseded `5cb33b0` [Linux 34660503559](https://github.com/CryogenicPlanet/artifactory/actions/runs/34660503559) and [QEMU 34660503627](https://github.com/CryogenicPlanet/artifactory/actions/runs/34660503627) predate later cleanup/conditional-write changes.

The clean `3d0206a` full suite finished **801 passed, six failed and one skipped in 587.63s**, across **189 passed, five failed and one skipped file (195 total)**, using actual Node 22.22.3 and two workers (`/tmp/comms-third-pass-full-3d0206a.log`). Clean check/build passed (`/tmp/comms-final-review-check.log`, `/tmp/comms-final-review-build.log`), but this is not full acceptance. Three saved-good page/source fallback failures exposed a confirmed production regression: the item 38 startup guard rejected source-only publishing before known-good selection. Integrated `42a1a9c` corrects that boundary; its worker reports 86 passing tests. Root source/health verification passes 36/36 across four files in 60.72s with one worker (`/tmp/comms-final-fallback-focused.log`).

The other three full-suite failures are tracked separately: `c96e4a0` corrects request-event assertions for the item 40 direct-boot contract (5/5 focused passes); the optional roster example consumed app-visible `http.request`, corrected by integrated app-activity example `5f3a9da` (1/1 in 1.83s); and the supervisor fixture expected lock HTTP409 where item 39 permits HTTP200, corrected by test-only `f6da946` (1/1 in 31.81s, worker check passes). Corrected production check/build pass at `5f3a9da` (`/tmp/comms-corrected-review-check.log`, `/tmp/comms-corrected-review-build.log`). The second full suite at `f6da946` finished **806 passed, one failed and one skipped (808 tests)** across **193 passed, one failed and one skipped file (195 total)** in **558.48s**, with actual Node 22.22.3 and two workers (`/tmp/comms-third-pass-corrected-full-f6da946.log`). Its sole failure was an invalid webhook assertion requiring every delivered actor to be `codex`; `5ff231e` corrects the public multi-actor expectation and passes 4/4 focused tests in 9.18s, check and fresh review. The third full suite at exact code checkpoint `5ff231e` **passes: 807 tests passed and one skipped (808 total), across 194 passed files and one skipped file (195 total), in 559.53s**, exit 0, with actual Node 22.22.3 and two workers (`/tmp/comms-third-pass-final-full-5ff231e.log`). This ledger follow-up is documentation-only. Exact clean `5ff231e` check/build pass (`/tmp/comms-third-pass-final-check.log`, `/tmp/comms-third-pass-final-build.log`). Owner UI, runtime and manifest changes remain dirty, preserved and excluded from clean-checkout acceptance.

Item 40 wait/privacy changes remain integrated. The webhook fixture correction `bb33ec8` passes 4/4 with actual Node 22.22.3 and one worker in 9.41s, reflecting the intentional exclusion of request logs from app queries. Item 43 integrates the canonical digest example (`1f8f91b`), schema-derived SQL guard (`d2935e3`) and six row-decoder substitutions (`2958d7e`), with check and fresh review passing. Subscription response arms remain separate for TypeScript narrowing without a kernel-error import; local sync closures retain their existing safety/scope boundaries.

Historical `5bbfbad` Linux finished 788 passed, one failed and one skipped; image and QEMU passed. Its stale minimal-api root-mark expectation was corrected in `cc31bee` with two focused passes. Linux at `cc31bee` is terminal: **792 passed, seven failed and one skipped (800 tests)**; image and QEMU pass. Failures include the webhook contract mismatch corrected by `bb33ec8` and a backup fixture’s 30-second aggregate timeout with unknown stalled phase. Test correction `b65c2ee` retains the first real five-second health timeout and makes subsequent retry failures immediate; it does not diagnose the historical stall or establish final-head CI acceptance.

At `0463745`, [Linux run 34652463298](https://github.com/CryogenicPlanet/artifactory/actions/runs/34652463298) is terminal: **806 passed, one failed and one skipped (808 tests)** across **193 passed, one failed and one skipped file (195 total)**. Shard 1 has 442 passes and the webhook every-actor assertion failure in 937.64s; shard 2 passes 364 with one skipped in 760.56s. Image and QEMU pass. The sole failure is the same fixture corrected by `5ff231e`. Corrected `5ff231e` is pushed; [Linux run 34653806152](https://github.com/CryogenicPlanet/artifactory/actions/runs/34653806152) and [QEMU run 34653806142](https://github.com/CryogenicPlanet/artifactory/actions/runs/34653806142) were pending at that checkpoint and are superseded by final-head verification above.

Earlier `5bbfbad` combined auth verification passed 28/28 tests across seven files in 27.69s using actual Node 22.22.3 and one worker, with check. It corrected the initial group’s 20 passes/four health-fixture failures by routing auth diagnostics to stderr-only Console instead of the injected generic event-store sink; durable `http.request` records remain. This evidence does not substitute for final combined acceptance.

The `23b9be5` full run finished **780 passed, one failed and one skipped in 558.89s**. The generation-revert fixture stalled during initial startup at attempt 0, before any revert action; the cause is unknown. This follows the earlier stream fixture correction `7064136` (15 focused passes) and health-timeout retirement fix `23b9be5` (23 focused passes, including the baseline `frozen:true` regression). Those focused passes do not diagnose the later startup stall.

### Remaining review boundaries

Remaining base work: resolve and verify fourth-review items 44–50, compose reviewed isolated fixes against current code, and repeat combined check/build/full-suite and Linux/image/QEMU acceptance. The previous accepted-cutover cleanup fix, conditional PUT contract and calendar/legacy removals remain implemented; their new review findings remain open. Do not dismiss item 46 merely by repeating earlier authority-refusal rationale: the confirmed repair-admission defect needs a safe fix. Likewise, durable table protection remains necessary while item 48’s migration bypass and lifecycle questions are unresolved. Database PRs #2–4 have resumed by explicit user direction; no blanket review-resolution or base-completion claim is made.

The disposable user trial verified actual passkey registration/login and board/API HTTP200 responses while preserving its existing data. The share proxy had rewritten Origin and produced a confirmed HTTP403; a temporary fixed-origin adapter corrected that trial transport without weakening boot's strict Origin check. This narrow trial does not establish full-suite or deployment acceptance; no credentials or setup code are recorded here.

The integrated scope remains: `dd3c6e7` removes Prometheus presentation and child-span aggregation; `ba5866b` replaces anchored editing with conditional raw writes while preserving source versions and safe revert; `2ee17ac` narrows public boot events and moves rich app event browsing into the app. The test-only orphan-fixture correction is `39e0bd0`. Earlier lifecycle/discovery/obsolete undo-coordinator changes remain integrated.

Public `/_boot/events` now requires read scope, accepts `since`, `limit` and bounded `wait`, and exposes trusted boot lifecycle/failure records plus boot-written `http.request` records. Request records are limited to the authenticated caller’s agent; human sessions may read all. Private failure details (`current_failure`) still require a human session or fs scope and project the generation row’s current bounded/redacted detail, not immutable historical stderr. The allocated-sequence diagnostic cursor keeps boot failures visible behind a pending app publication fence and must not resume application events. App-owned `/api/events` and `/api/stream` retain rich filtering and delegate waiting to the authenticated private boot transport’s commit signal; app drain ends waits and clients resume from their app cursor. Private child queries exclude all `http.request` rows regardless of child-supplied actor parameters. This deliberately narrows SPEC’s whole-log app browsing promise while retaining stored events, publication/replay evidence and private-header stripping. Retained internal consumers keep the private transport, but old snapshots without app-owned `/api/events` still need an editable route update for that public feed. Safe startup, messages and source repair remain required.

**Historical combined local acceptance, before the latest fixes:** `39e0bd0` passes **780 tests with one opt-in skipped**, across **190 passed files and one skipped file (191 total)** in **534.13 seconds**, using actual Node 22.22.3 and **two workers** (`/tmp/comms-ownership-corrected-full.log`, exit 0). Root check passes (`/tmp/comms-ownership-corrected-check.log`); production `2ee17ac` build and fresh combined review pass. This full run predates `7064136` and the production health-timeout fix `23b9be5`. At `39e0bd0`, image and QEMU pass; Linux is now terminal with 779 passed, one failed and one skipped (781 tests): shard 1 passes 439, shard 2 has 340 passed, one stream-fixture failure and one skipped. The explicit-gate correction addresses that fixture; it is not new-head Linux acceptance. Consult PR checks for the subsequent head.

The initial `2ee17ac` full run finished 779 passed, one failed and one skipped in 544.12s (`/tmp/comms-ownership-final-full.log`). The custom orphan child fixture lacked the now-app-owned `/api/events` route; JSON parsing failed after closure and exact orphan-row assertions had passed. Test-only `39e0bd0` reads authoritative published boot-store evidence and verifies exact-once event, published batch and publication fence instead. Six focused cases pass; production is unchanged. This fixture contract mismatch is distinct from an established production regression. Prior `a834e3f` acceptance (782 tests plus one skip, 519.49s) remains historical evidence.

A separate historical Linux gap remains: the canceled `2ee17ac` run’s saved shard-2 log contains a 15-second public-path policy fixture timeout (15,025ms). That case uses 27 sequential Bun subprocesses, but has no active-phase evidence; its cause is unknown and no fix or resource-pressure diagnosis is claimed. The terminal `39e0bd0` Linux result is recorded above; it does not establish the cause of the earlier public-path timeout.

Page seeding was investigated and intentionally not moved: boot’s one-time marker protects deleted/restored data, and moving it safely would require a new launch protocol. Calendar retention is retired; generic log mirroring and manual token issuance remain unimplemented audit removals, not blockers to the current agreed diagnostics/source-versioning slice. Their retention does not imply all ownership-review suggestions are resolved. Source version inspection and safe revert remain required throughout.

**Separate acceptance measurements:** the source-boot fixture launching the actual kernel passed under Node 22.22.3 and Bun `1.4.0-canary.1+4924862cf`: 52 requests, 25 acknowledged writes preserved with exact idempotent replay, 50 HTTP200 responses and two GET503 `stale_writer` refusals marked retriable during healthy/repair swaps. No POST or transport failure occurred. Recorded freezes were 98/95ms; request p95 was approximately 10.89ms and maximum 101.10ms. This is not a compiled-image/load benchmark or exact step-6 timing. The first harness rejected the permitted stale-writer refusals; its artifact remains retained, and the harness was corrected against SPEC §7.7. Do not call the result zero refusals.

The temporary two-agent onboarding/conversation/edit workflow passed in 2.02s with one worker at `d18a3f1`, after correcting its page-seed and Effect fixtures. It used public operations with no database inserts. Evidence: `/tmp/comms-agent-workflow-acceptance/result.json`. These earlier bounded runs supplement baseline evidence; they do not accept later scope changes or final-head Linux/image/QEMU behavior.

**Historical Linux gap:** `bbb4da9` finished 716 passed, one failed and one skipped. The SQL fixture exceeded its 30-second aggregate while issuing 43 requests. Test-only `7e1501c` separates failure stress from bounded-query assertions, retains the same 30-second deadline, and passes its three focused cases. This does not establish the historical timeout's cause. The earlier `854e18c` first-message HTTP507 before QEMU reboot also remains unexplained; later green runs do not diagnose it. Final pushed-head Linux, image and QEMU acceptance remain required.

Fresh app stores no longer create retired reaction tables; existing historical data remains preserved. Legacy boot topic-move recovery is now refused on table presence by `8cc783e`; old tables and page trees remain untouched, and pruning is disabled on that refusal path. Ordinary supported live paths use app-owned moves. Earlier legacy reconciliation and completed-orphan test evidence describes superseded behavior, not a current migration promise. Raw line count is not acceptance: preserve supported recovery guarantees and state unsupported legacy-store boundaries explicitly.

Readonly SQL uses scoped subprocess cancellation; synchronous SQL writes still rely on whole-child keeper/watchdog recovery. Cooperative read cleanup escalation is not a hard wall-time guarantee for arbitrary synchronous code. Request diagnostics remain intentionally lossy. Native installation/iOS and physical power-loss behavior remain untested; ordinary process-group closure excludes escaped/adversarial sessions. Do not claim every review comment is resolved.

At the efa6de5 verification checkpoint, 20 protected owner-document hashes were unchanged (`/tmp/comms-final-base-owner-doc-hashes.json`); the earlier second-pass inventory contained 18. Subsequent owner review/UI commits are newer work, not covered by that hash comparison. Preserve `SPEC.md`, `docs/tech.md`, `docs/database.md` and `docs/pr-1/`. The second-pass inventory `/tmp/comms-base-owner-hashes-second-pass.json` supplements original inventories; owner revisions are not agent edits. Older 16/17-file counts below describe their checkpoints, not today's file set. Original provenance and frozen handoff hashes remain historical evidence; never replace newer files with those snapshots.

## Historical checkpoints — superseded

Everything below records earlier states and evidence. “Current”, “latest”, “remaining” and worker instructions in these historical records apply only to their named checkpoint; the leading section and build plan control present work. All four original frozen handoffs were later integrated after hash verification. Do not restart or reapply historical lanes.

Owner-file preservation: 15 of the original 16 files still match `/tmp/comms-takeover-owner-hashes.json`. The owner revised `docs/database.md` and added `docs/pr-1/stack-review-2b3e365.md`; both are preserved. The current 17-file inventory is `/tmp/comms-base-owner-hashes-854e18c.json`. The original inventory remains unchanged. Earlier claims that all 16 still matched at `854e18c` were incorrect.

Current base work: fresh numbered-review audit, required shared boot-route/Origin policy, and diagnosis of QEMU run34617471582 failing its first message with HTTP507 before reboot. The image job103322880489 passed; Linux run34617471531 is being inspected separately. The QEMU failure has no captured error code yet; neither a startup measurement race nor actual low capacity is established. No database-stack fixes are being advanced.

Fresh base audit at `854e18c` identifies confirmed unfinished requirements: shared boot-route/Origin policy (item19/21), cancelable SQL statement execution beyond the existing child watchdog (item15), and remaining handwritten UI component CSS (item20). Isolated writers own these; no database-stack writer is active. Structural boot reduction remains open. SQL's HTTP adapter stays in kernel per the explicit SPEC §6 route table; do not relocate it merely from a broad ownership inference. Historical reaction rows remain as a preservation qualification. Existing restart/metrics/combined-restore/startup-graph/gate-finalization code contradicts the corresponding stale stack-review claims.

Diagnostic-only `9ad07fa` adds bounded allowlisted HTTP/storage evidence to the QEMU guest after its original HTTP507 refusal. Root check, Python syntax and fresh review pass; no retry, deadline or acceptance assertion changed. Current 17-file owner inventory remains unchanged. The original QEMU cause is still unknown.

**Latest local base acceptance at `3c9bace`:** scoped file-sync cleanup and stateless row decoding remove48 production lines while preserving scopes/errors/transactions; boot now12,158lines/94files. Fresh combined review/check/build pass. Full actualNode22.22.3/two-worker run passes712/712 across177files in513.66s, log `/tmp/comms-final-simplicity-full.log`. Later diagnostic-only headda7cb75 adds strict storage-response and pre-teardown cutover-phase evidence; check and9/9 tests in both affected files pass20.88s (one worker alongside isolated packaging validation). Exact updated Linux acceptance is pending. Includes PR3 marker diagnostics and a three-line Linux keeper fixture correction: after positive receipt, one writable read recovers a killed DELETE-mode writer’s hot journal before unchanged readonly stability checks. Linux PR3 hit exactSQLITE_READONLY_ROLLBACK776; native SQLite reproduction confirms that mechanism, local Bun canary did not reproduce it. No timeout/retry/production fix is claimed.

**Latest base acceptance:** production code `06ae030` passes local check/build and711/711 tests across177files in485.24s with actual Node22.22.3/two workers (`/tmp/comms-cleanup-final-full-tests.log`). Test-only corrections at pushed `c1a1649` pass the full Linux suite **712/712 across177files**: shard1 376/376 in842.58s, shard2 336/336 in656.16s, [run34605177692](https://github.com/CryogenicPlanet/artifactory/actions/runs/34605177692). Both check/build and serial diagnostics pass. Actual [QEMU run34605177608](https://github.com/CryogenicPlanet/artifactory/actions/runs/34605177608) passes on that head: real changed kernel identity, old open attempt without receipt, exact acknowledged rows/idempotent response, and new writes afterward.

Image acceptance passed at `c4ecc7d` ([job103275910537](https://github.com/CryogenicPlanet/artifactory/actions/runs/34603394894/job/103275910537)): WAL privacy, boot/app/build UIDs/capabilities, helper denial, ordinary descendant closure, result reclamation, HTTP and page/database restart persistence. At `c1a1649`, keeper acceptance passes again but smoke fixture's external CREATE TABLE hits SQLITE_BUSY because it had no busy timeout. Reviewed `3324d8a` adds only `PRAGMA busy_timeout=2000` to that external fixture connection. Deterministic lock-release500ms succeeds; held3500ms fails at~2s. Exact persistence assertions remain. **Latest exact-head acceptance is green at `5d96c1d`: [Linux run34606806535](https://github.com/CryogenicPlanet/artifactory/actions/runs/34606806535) passes712/712 across177files (shard1:376/376,804.49s; shard2:336/336,657.24s), all check/build/diagnostics and actual image job103287124477. [QEMU run34606806574](https://github.com/CryogenicPlanet/artifactory/actions/runs/34606806574) also passes real kernel/data recovery.** No production change or arbitrary SQL retry is claimed.

Integrated base review work: all four frozen handoffs were hash-verified and composed against newer code; startup/admission renames were not reapplied. Human reset/undo preserves messages, pages and identities, borrows edit ownership with revalidation, and retains exact durable replay outcomes. Actionable errors, canonical token/proof fields, optional deletion/profile/observed-roster examples, signed settings, bounded receipts, physical event accounting, artifact retention, standalone workspace preparation, rehearsal reports/rollback, generated AtomHttpApi/scoped SSE, immutable recovery UI, PWA, development page polling, System mirror, seq.reserved diagnostics, request tracing and bounded Logger export are integrated. Linux UID/store-layout/keeper recovery and QEMU acceptance are implemented.

Final named cleanup shares commit-time authority and exact hex redaction; uses typed cutover values with partial recovery state; consolidates six schema modules while preserving SQL bytes/order and fixture setup. Fresh reviews found no blockers. Boot is12,206 production lines across93TypeScriptfiles; approximate7,250 target remains unmet. At that historical checkpoint the generic bootRoute/Origin table had not yet been implemented (subsequently integrated by d9224e7): routes retain distinct human/bearer/Origin and commit-time reauthorization boundaries. Existing helpers share credentials, bodies, proof decoding and failures. Row decoding and directory sync are now in the verified local cleanup above; do not claim all review suggestions are resolved.

Validation caveats: intermediate `57b961f` local run had709/711 with two initial startup failures, before edit actions. Exact pair9/9 and final full711/711 pass; original cause remains unconfirmed. Safe last-status/stderr diagnostics retain evidence without deadline changes. Linux `c4ecc7d` had707/711: three raw page setup503s and one legitimate late System message race. Test-only corrections scope the cursor topic and retry only503+publication_pending+retriable:true for2s. A controlled150msreservation proves refusal before page bytes/history/journal changes; originalLinux503bodies were not captured, so historical cause is not proven. These corrections now pass all712Linux tests. Logs remain in `/tmp/comms-linux-34603394894-clean.log` and `/tmp/comms-linux-34605177692-clean.log`.

Browser acceptance remains narrow: compiled board login/post/read, generated-client reconnect, settings draft/revision, source recovery with every child broken, PWA installability, development CSP/auth and mobile layout pass. Native mobile installation/iOS were not exercised. QEMU proves guest kernel reset, not physical power loss; process-group proof excludes escaped/adversarial sessions. Local Docker Desktop terms/privileged setup were never accepted.

**Separate database track:** [draft PR#2](https://github.com/CryogenicPlanet/artifactory/pull/2), branch `codex/database-store-descriptors-final`, commit `07af2c7` on `5d96c1d`, checkout `/Users/cryogenicplanet/general/comms-database-descriptors-final`. It adds stateless @comms/storage, APP_STORE with matching derived APP_DATABASE for retained generations, private-copy keeper rewriting and runtime/Docker packaging. No identity/DbOps/dialect/remote support. Final check/build and716/716 tests across179files pass473.50s actualNode22.22.3/two workers, plus real legacy rollback/restart and frozen standalone startup. PR2 latesthead `e954e70` passes [Linux716/716 across179files plus image](https://github.com/CryogenicPlanet/artifactory/actions/runs/34610021635) and [QEMU](https://github.com/CryogenicPlanet/artifactory/actions/runs/34610021779). The workflow now includes storage tests; the first713-test Linux pass had omitted those three tests. Earlier descriptor full was stopped after two manual-seed workspace omissions; both were fixed before final pass. Original checkout `/Users/cryogenicplanet/general/comms-database-descriptors` retains protected reference docs; no stash/reset was used.

[Draft PR#3](https://github.com/CryogenicPlanet/artifactory/pull/3), branch `codex/sqlite-store-identity`, head `10d3064` (implementation4ecf31c plus workflow merge), checkout `/tmp/comms-store-identity`, adds resumable UUID adoption and catalog-authorized legacy backup stamping on disposable copies. Check/build/fresh review and724/724 tests across180files pass479.84s actualNode22.22.3/two workers. At10d3064, image and real-kernel QEMU34611752691 pass, while Linux34611752910 finishes722/724. Boot page-move and server accepted-reset fixtures did not reach held markers; early request results were hidden, so the cause is unknown. Diagnostic-only head7d79b18 preserves deadlines/assertions, passes check and combined21/21 tests26.55s, and is pushed. Exact Linux34614367087 passes724/724180files (379/90/679.95s and345/90/715.65s), and QEMU34614367079 passes. Its image job103312483928 fails with readonly hot-journal recovery error776, addressed by the unpushed base fixture correction above. The marker failures do not recur; their original cause remains unknown. Unique private `.restore-*` directories may remain afterSIGKILL; automatic reclamation is not implemented. Existing isolated missing-store refusal is retained, with the prior nonisolated journal-authorized replacement path preserved. First identity full was stopped for canonical backup validation and one stale attempt-count fixture; only the final724pass is acceptance.

[Draft PR#4](https://github.com/CryogenicPlanet/artifactory/pull/4), branch `codex/sqlite-db-ops`, checkout `/tmp/comms-sqlite-db-ops`, pushedhead2b3e365, stacks onPR3. Commits27bfc55/090e3cd/fbe6c45/d9522da add descendant matching, schema18 engine provenance and descriptor-based DbOps. Final production check/build/freshreview and734/734 tests181files483.16s pass actualNode22.22.3/two workers. Head additionally merges PR3 diagnostic fixtures and passes check/21focusedtests26.44s. Linux34614609950 finishes732/734: storage-startup restored-policy message returns507, and server cutover slow-body test reaches its20s aggregate timeout. Both lack diagnostic cause; isolated workers add response/phase evidence without retries or larger deadlines. Image and QEMU34614609932 pass. Copy/restore preserve identity, canonical catalog paths, closure and journal ordering; foreign engines refuse beforecopy, survive retention and cannot receive SQLite legacy identity stamps. Production delta+38lines. No remote engine/pointer mutation support. Root stays on base branch. Owner-file revisions are recorded in the leading preservation inventory.

Paused database work: SQLite boot/core migration ledgers are committed in `/tmp/comms-migration-ledgers`, stack2b3e365..9cf7dc8, with120/120focusedtests18files56.19s, check/build/freshreview; fullsuite/integration pending. ExacthistoricalSQL/order and legacy version mirrors are retained; only absent ledgers permit legacy adoption, completeprefix/name/mirror validation refuses gaps/conflicts; `/root/database_plan` committed unpushed scoped remote clients/continuous inspectors at `a837719` in `/Users/cryogenicplanet/general/comms-remote-session-clients` basedd9522da. These are unintegrated; focused checks do not establish backend acceptance. Real remote startup/restart closure remains an explicit gap. Database design constraints are recorded in docs/build-plan.md: safe transfer completion/source retirement, resumable legacy identity adoption and authorized old-backup stamping, MySQL DDL recovery, separate migration ledgers and explicit transfer inventory/codecs. Identity plan additionally needs a durable adoption intent reserved before app UUID write and pre-adoption backup provenance; missing initialized stores must never be recreated. Owner docs remain unchanged. Do not infer implemented database support from these plans.

## Previous verified takeover — 2026-09-11

Resumed from `51eef7a` on `codex/build-comms-core`. The owner files `SPEC.md`, `docs/tech.md`, `docs/database.md`, and `docs/pr-1/` remain uncommitted and byte-preserved. Historical checkpoint paragraphs below are superseded by this section.

**Latest acceptance — tested code `fb72973`:** actual Node22.22.3 with two workers passes **643/643 tests across 159 files** locally in 441.28s (`/tmp/comms-harness-final-root-tests.log`). Linux run [34591769316](https://github.com/CryogenicPlanet/artifactory/actions/runs/34591769316) passes **643/643 across 159 files**: shard 1 338/338 in 627.21s; shard 2 305/305 in 624.68s. Both Linux check/build and all three serial startup diagnostics pass. Root check, frozen install/build and the fresh combined review also pass. The final follow-up commit changes only this acceptance ledger and build-plan documentation. All 16 protected owner-file hashes still match the takeover inventory.

All four frozen handoffs were hash-verified and deliberately composed against newer code: source-only seed reset (schema 16 and borrowed staging preservation), unpaginated account metadata, boot discovery including signed restart/combined restore/reset, and boot/auth request diagnostics. Startup/admission renames were already present and were not reapplied. Shared proof/keeper schemas and an EditLock upsert remove 23 production lines; this is not completion of the boot size target.

Subsequent reviewed fixes: ordinary app descendants must close before the keeper issues a receipt (a real SQLite writer continued after the old receipt); keyed source undo records exact terminal outcomes, atomically with app acceptance or bound to page publication, so replay cannot overwrite later edits or rerun hooks. Legacy selection-only keys refuse unknown outcomes. Receipts remain unpruned. UI read state uses scoped Effect Atom/AsyncResult registries with a shared identity query; generated AtomHttpApi and SSE invalidation remain pending. A real browser smoke passed login, posting, draft preservation, account/extensions and message references. Manual app onboarding routes now self-list.

Remaining explicit gaps: approximately 7,250-line boot target (current 10,719 production lines / 87 files; baseline 10,029; new retained capabilities add code), physical main-file/WAL event quota, signed settings, genuine rehearsal suppressed-call reporting, Linux UID/build isolation and real reboot acceptance. Ordinary process-group closure excludes escaped sessions/adversarial descendants. Database portability remains a separate PR. Do not claim all review comments are resolved.

### Validation history (superseded by latest acceptance above)

Local validation uses actual `/Users/cryogenicplanet/.vite-plus/js_runtime/node/22.22.3/bin/node` and two workers. Local Bun revision remains `1.4.0-canary.1+4924862cf`; Linux pins the published release.

- Initial full baseline: **568 passed, 7 failed, 575 tests / 152 files**, 414.62s (`/tmp/comms-takeover-baseline-tests.log`). All seven were reproduced stale assertions and corrected without relaxed deadlines: malformed combined restore, durable lifecycle events and inventory waiting for terminal startup failure.
- Combined handoffs: **608 passed, 3 failed, 611 tests / 156 files**, 486.79s (`/tmp/comms-takeover-handoffs-full-tests.log`). Page undo hit the known prepublication refusal; the fixture now waits for actual successful publication before lost-response assertions. Deferred lock-break recovery expected the wrong event; a focused correction separately proves interrupted recovery. The first cold-manifest reload returned failed after about 92s, with its error hidden by the assertion; added response diagnostics, without claiming the cause or changing deadlines. That case passes in the undo worker's full generation group.
- Root frozen install/check/build pass after keeper/UI/undo composition. Earlier root focused runs passed 32/32, 48/48 and the exact request-Scope/onboarding checks. Isolated keeper passed 25/25; undo passed 38 distinct tests / six files; lock fixture passed 23/23. These overlap and are not final root full acceptance.
- Final combined full suite at `76820b7` (code unchanged in pushed `7d4ce61`) passed **626/626 tests across 159 files**, 449.09s (`/tmp/comms-takeover-final-full-tests.log`), actual Node22.22.3 and two workers. Fresh combined keeper/UI/undo/reset review found no actionable findings. All 16 owner-file hashes still match the initial inventory.
- Checkpoint Linux run [34583134298](https://github.com/CryogenicPlanet/artifactory/actions/runs/34583134298) passed check/build/serial diagnostics but exceeded the 20-minute job limit. Completed reports show **448 passed, 17 failed / 465 cases**, not a full-suite result. Ledger `/tmp/comms-linux-34583134298-ledger.md`. CI now uses two shards, two workers each, unchanged runtime pins and 20-minute limit, with verbose immediate failures; Linux run [34585848966](https://github.com/CryogenicPlanet/artifactory/actions/runs/34585848966) at `7d4ce61` completed: **623 passed, 3 failed / 626 tests across 159 files**. Both shards passed check/build; shard 1 passed 338/338 in 567.43s, shard 2 passed 285 and failed 3 in 573.48s. Remaining failures are the child-identity matrix aggregate 5s budget, the generation dependency-preparation/repaired-retry aggregate 60s budget, and page-undo crash injection never reaching its marker. Follow-up fixtures split the seven independent identity cases without increasing their deadlines and deterministically reproduce/handle an early `publication_pending` refusal before the undo crash marker. The preparation case now records bounded phase timings; its Linux stall remains unknown. No production deadline was relaxed. Logs `/tmp/comms-linux-34585848966-clean.log`.

Follow-up root validation passes **10 selected tests across three files**, 34.34s, plus check (`/tmp/comms-linux-followup-root-tests.log`, `/tmp/comms-linux-followup-root-check.log`). These fixture-only changes add six separately reported identity cases; the previous 626-test full pass predates them. Linux rerun remains required. Linux ordinary descendant closure tests passed 4/4, child keeper 5/5 and preparation process 4/4; this does not establish UID isolation or reboot behavior.

Linux follow-up [34587285260](https://github.com/CryogenicPlanet/artifactory/actions/runs/34587285260) at `3dc553b` completed **621 passed, 11 failed / 632 tests across 159 files**. Both check/build passed; the earlier identity and page-journal fixes passed. Generation preparation passed at 58.657s: cold readiness 26.127s, failed preparation 2.094s, repaired preparation 29.127s. Its aggregate budget now accommodates both full preparations (120s); production/readiness deadlines are unchanged. Source replay adds phase evidence and a 45s aggregate for three cutovers plus restart; its prior Linux stalled phase remains unknown.

The other fixture corrections split independent lock/browse/path-rejection cases (11 additional reported tests), await terminal attempt 3 before exact health/event and inventory assertions, use explicit bounded child readiness instead of default 1s polls, and give the 20-process protected-event scenario 15s. The held-body auth fixture now waits for actual old-channel invalidation within its original 1.8s bound before completing the body, then separately checks replacement readiness. No production code changed in these CI follow-ups. Root validation passes **90/90 tests across all ten affected files**, 108.08s, actual Node22.22.3 and two workers; check also passes (`/tmp/comms-linux-second-followup-root-tests.log`, `/tmp/comms-linux-second-followup-root-check.log`). That checkpoint did not achieve Linux acceptance.

Linux [34588957641](https://github.com/CryogenicPlanet/artifactory/actions/runs/34588957641) at `481a878` completed **642 passed, one failed / 643 tests across 159 files**. All previous failures passed, including source replay at 20.186s and 20.306s (above its former 20s aggregate). Only metrics failed its default 1s pre-freeze wait. A controlled 1.5s valid candidate startup reproduced that same failure before rehearsal completed; the corrected explicit 5s wait passes, retains held-upload/queue/drain/swap assertions, and records bounded phase diagnostics. Production drain/readiness limits are unchanged. Final exact-root acceptance at `f8b4e6b` passes **643/643 tests across 159 files**, 439.54s, actual Node22.22.3 and two workers (`/tmp/comms-final-643-root-tests.log`). Root check and six targeted metrics/Scope/traffic tests also pass. All 16 protected owner-file hashes still match. Linux run [34590402242](https://github.com/CryogenicPlanet/artifactory/actions/runs/34590402242) finished **641 passed, two failed / 643 tests across 159 files**. The metrics fix passed; remaining failures were a default 1s listener wait before mint/logout behavior began and a default 5s aggregate for legacy schema-three migration. Both now have explicit setup allowances matching equivalent fixtures.

Repeated Linux failures demonstrated that the generic unit-test defaults were unsuitable for the real-process integration harness. After a fresh read-only deadline audit, root Vitest defaults now use two workers, a 15s aggregate and 5s eventual-state polling. This explicitly relaxes incidental observation bounds; it does not establish that every earlier timeout was harmless. Protocol timing tests retain TestClock checks, elapsed-time assertions, AbortSignal limits and explicit per-test overrides. Root deadline/auth/recovery validation passes **40/40 tests across ten files**, 60.62s, plus check (`/tmp/comms-harness-deadlines-root-tests.log`, `/tmp/comms-harness-root-check.log`). Final full acceptance at `fb72973` passes locally and on Linux, as recorded above.

## Agent handoff — owner requested stop and checkpoint

The owner requested a fast wrap-up because usage is low and will transfer remaining work to another agent. **Do not interpret this checkpoint as completion.** This section supersedes older status paragraphs below.

Integrated in the checkpoint based on `2979676`: SSE/preparation/metrics and Linux fixtures; corrected core ownership using the actual public extension read/mutate API; numeric query schemas; actual HttpApi error encoding; combined source/database restore; terminal health reporting; boot literal errors/durable events/signed restart; queued restore requests with credential/grant revalidation; auth full-Cause rollback hardening; removal of boot-to-app page-write policy; raw journal/undo concurrency fixes; optional digest example; concrete startup graph and all three admission renames. Known source conflicts retain actionable nonretry409; unexpected/mixed failures produce nonretry500.

**Validation at handoff:** root frozen install, check and build pass. The installed patched Bun adapter SHA256 is `5ee2f05cb5ab5bb5a50c73e6aa2ed2bc58aa41c666bcad2dc79a9f1cab97b341`. Root core acceptance passed17/17 tests in six files before the later95-path boot integration. That boot integration passed42 final boundary tests plus check/build and fresh reviews in isolation. The final startup composition passed88/88 tests across17files, check/build and fresh review in isolation; its exact34 afterimages were then applied to root. The earlier519/519 full root suite predates these new bundles. **The final combined checkout has not had a full test run or Linux acceptance.** Logs: `/tmp/comms-boot-policy-root-install.log`, `/tmp/comms-handoff-root-check.log`, `/tmp/comms-handoff-root-build.log`, `/tmp/comms-core-wave-root-tests.log`.

Next agent should first run the full retained suite with actual Node22.22.3 and two workers, review current CI, then integrate remaining handoffs deliberately. Preserve the owner's uncommitted `SPEC.md`, `docs/tech.md`, `docs/database.md`, and `docs/pr-1/`; they are excluded from this commit. Do not replace them with worker copies.

Remaining work and reusable handoffs:

- **Startup graph and admission renames are already integrated; do not apply again.** Final handoff: `/var/folders/2j/z4115t_51pv7sxxrq1_j7_xh0000gn/T/comms-startup-final-swv7zfjf/handoff`, manifest `89fc6e3bef6504aa2a6e4bff9bbbc9eb7f002e4d19ababcd127672298e494fbe`. It removes seven nullable service Refs and proxy positional arguments. Preserve its proven `Context.pick` fix: capturing runtime Scope in the installed proxy leaks request leases. Older startup35 handoff is superseded.
- **Source-only seed reset** is frozen but unintegrated: `/var/folders/2j/z4115t_51pv7sxxrq1_j7_xh0000gn/T/comms-source-reset-k7sqa9no/handoff`,32 paths, manifest `6c6eb38b9d176b9db9ab127e45d44671e6dc6931c5b1027c05fdd2fb8321e8b4`. Base is the older combined-restore handoff. Preserve newer auth/event/page/startup changes; schema16/reset_pin must compose deliberately. Reset preserves messages, pages and identities.
- **Simpler account listings:** `/var/folders/2j/z4115t_51pv7sxxrq1_j7_xh0000gn/T/comms-account-simple-2fs9_qdn/handoff`,8 paths, manifest `0c02de70c229e6aa905eb1f0d378807330ad07eb85241b60fe6f19f48d015834`.
- **Boot discovery descriptors:** `/tmp/comms-boot-discovery-handoff`,5 paths, manifest `b374fe364174cc12686d7f67d109989408b46fcc313f16b7f10f36820320895a`. Update descriptors for now-implemented restart/combined restore; inspect notes about app self-listing.
- **Request diagnostics covering boot/auth while app is down:** `/private/var/folders/2j/z4115t_51pv7sxxrq1_j7_xh0000gn/T/comms-request-diagnostics-hupm98vw/handoff`,4 paths, manifest `681aed1984481fd78c2bff99637fc18c33536bb42d7907955de10c359d0b510e`. Preserve actual request scope and exclude feed self-logging when composing with startup graph.
- Further boot consolidation remains: canonical proof/IPC/schema/route duplication and cheap EditLock upsert. The approximately7,250-line boot target is **not achieved**; do not claim relocation or state cleanup meets it. UI state choice still needs reconciliation with the owner's tech document. Rehearsal suppressed-call reporting is missing. Physical main-file/WAL event quota and Linux/container UID/descendant/reboot acceptance remain unestablished. Source-undo exact outcome replay and signed settings still need narrow conformance decisions. Database portability stays a separate future PR.

The applied coherent manifests are `/var/folders/2j/z4115t_51pv7sxxrq1_j7_xh0000gn/T/comms-core-restore-wave-vockzoo9/handoff` (156 paths, `93d1d005f53b7d1c8c0058338c6beabd5eb34a3c9f7562eb5f9959ed48ecfcd0`) and `/var/folders/2j/z4115t_51pv7sxxrq1_j7_xh0000gn/T/comms-boot-policy-wave-2jx_n79s/handoff` (95 paths, `796f0e5ed671bb364ff74e67b012617d7143a142c0f46207c8cda693b473605c`). Their NOTES distinguish final accepted runs from superseded failure logs. Never blindly apply an old beforeimage over this checkpoint.

## Historical checkpoint: direction correction

The pushed PR head is `297967650e28770dacf817a94f6d015fed70cb28`. The current uncommitted checkout additionally contains the 30-path SSE/preparation/metrics handoff, 11 Linux fixture corrections and deployment documentation. Exact-root check/build pass, and the full retained suite passes **519/519 tests across 136 files**, 390.17 seconds, using actual Node 22 and two workers (`/tmp/comms-later-linux-root-tests.log`). This is local regression evidence, not architecture completion or Linux acceptance.

The corrected 156-path core/typed-query/error/terminal-health/combined-restore handoff is now also applied locally, with every before/after hash verified. Core consumes public read/mutation capabilities, rather than privileged message/topic services; unused public mutation guards are rejected instead of exposing a nested-read deadlock. Root check/build and **17/17 focused tests across six files** pass in 27.36 seconds (`/tmp/comms-core-wave-root-tests.log`), and a fresh root architecture review found no actionable findings. The worker's 119 distinct passing tests across runs are separate evidence. The preceding 519-test full run predates this integration; full acceptance of the newer combined checkout remains to run.

That pushed head's Linux run [34578266101](https://github.com/CryogenicPlanet/artifactory/actions/runs/34578266101) finished **500 passed / 14 failed across 514 tests and 133 files**, 907.92 seconds; check/build and three serial diagnostics pass. Nine failures correspond to the fixture corrections already in the uncommitted root; one corresponds to the pending terminal-initialization health fix. Four remain uncovered: a broken-startup aggregate budget, corrupted-entry retry polling, stderr readiness polling and a six-cutover tree-revert timeout. The last passes locally in 12.09 seconds with its unchanged 60-second budget; Linux's stalled phase is unknown. Evidence: `/tmp/comms-linux-34578266101-ledger.md`. None of the pending fixes is Linux-validated by this run.

The owner's new direction check, `docs/pr-1/direction-check-2979676.md`, takes priority over landing more routes:

1. Core must consume the public extension API. The frozen 104-path ownership relocation still leaves privileged service dependencies and is insufficient. Its replacement must put domain semantics on the same public read/mutation capabilities extensions use; do not add one public wrapper per HTTP route merely to disguise those dependencies.
2. Boot must never call the app for page-write policy. Remove the callback, publication-fence polling and app-policy gates. Preserve raw `/_boot/fs` and `/api/fs` repair aliases, authentication, safe paths and journal/recovery ordering. Those privileged raw edits do not enforce archived/deleted-topic policy; app-owned operations enforce their own domain rules. Do not silently route recovery aliases through the editable app or introduce an actor-relay framework.
3. Requests queue during database restore. Preserve the existing bounded queue and timeout; revalidate credentials and anonymous grants after waiting, and prove queue/resume against a real restore.

Separate isolated workers own these three corrections. No additional route wave is to be committed before the core public-API correction. The digest example is actually absent and has a bounded restoration worker. Numeric query schemas, startup nullable references/positional arguments, the three admission renames and remaining auth consolidation still need completion.

Historical size estimate omitted: its counting basis was not recorded and has not been reproduced. This checkpoint is superseded; current ownership decisions concern responsibilities, not line targets. Page journal publication still needs generic exclusion against active app reservations to prevent concurrent physical move/journal conflicts; this is a local ordering check, with no app callback or retry loop.

Other reviewed handoffs are ready or being composed in isolation: actual HttpApi error encoding; boot literal errors and nonretry defect propagation; durable boot transitions; signed restart with bounded shutdown; combined source/database restore; boot discovery descriptors; broader request diagnostics; simpler account listings. Source-only seed reset is still being validated. Their individual test counts are not root acceptance. Rehearsal suppressed-call reporting and Linux/container ownership/reboot acceptance remain explicit gaps.

Older checkpoint descriptions below are historical; this section controls current status.

## Prior checkpoint and authoritative review

- Repository: `CryogenicPlanet/artifactory`; the old `CryogenicPlanet/comms` git remote redirects there.
- Branch: `codex/build-comms-core`.
- Pushed checkpoint: `181939b` — `Checkpoint restore, topic move and source watcher integration`.
- Draft PR: <https://github.com/CryogenicPlanet/artifactory/pull/1>.
- Owner-approved review and ranked requirements: <https://github.com/CryogenicPlanet/artifactory/pull/1#pullrequestreview-5175391307>.
- Read the current `SPEC.md`, `docs/tech.md`, and `docs/pr-1/` before changing a new area. The review supersedes the old feature-completion plan wherever they disagree. `docs/build-plan.md` now records the review-aligned groups and retained safety contracts.
- The owner's uncommitted `SPEC.md`, `docs/tech.md`, `docs/database.md`, and `docs/pr-1/` were deliberately excluded from my checkpoint. Preserve them; do not replace them with worker beforeimages.

The checkpoint includes signed database restore, whole-source generation restore, topic move coordination, typed source history and direct-source watching, plus earlier authentication, conversation, UI and extension work. Several of these mechanisms are now explicitly selected for removal or relocation. Do not finish the old plan blindly.

## Historical integration and validation

The last pushed checkpoint above is historical. The following review changes are committed in `f2bec7b` (fixtures), `6e9a98e` (reactions) and `c6f2a14` (boot reduction and diagnostics):

- Six stale migration/event/page fixture corrections: 30 focused tests passed.
- Recovery diagnostics remain available after unproven keeper closure, without expiring locks or removing staging; mutations remain refused. Worker validation: 27 tests across four files, fresh review clear.
- Reactions removed from core routes, services, UI and docs. Existing database rows and legacy receipts remain intact for compatible migration. Worker validation: check/build and 25 tests across eight files, fresh review clear.
- First boot reduction removes watcher/auto-adoption, weekly backup drill and storage dashboard/cache: **609 fewer production lines**, 27 changed/deleted files. Hourly backups and source history/undo remain. Worker validation: check/build, 92 distinct retained tests across runs and fresh review clear.

Before the boot reduction was integrated, repository check/build passed and the full suite produced **402 passed, one failed (403 tests, 104 files)**. The remaining failure was `generation-revert.test.ts` initial readiness: child still `starting` after the fixture's 15-second deadline, before the edit under test. It is not yet diagnosed as a product failure or resource-pressure flake. After the reduction, check/build and all 22 focused integration tests across four files passed, including generation restore. No deadline was relaxed; the isolated rerun does not establish the original timeout cause. A fresh read-only integration review found no blockers.

A newer Linux run at `0813fe6`, [34570039082](https://github.com/CryogenicPlanet/artifactory/actions/runs/34570039082), passed check/build and the three serial diagnostic cases; its four-worker suite finished with 309 passes and 82 failures. New diagnostics showed a nine-subprocess crash case reaching recovery only after its five-second test budget. The follow-up at `6c21a88` uses two workers, five seconds only for initial auth listening, and 15 seconds only for two source crash groups; root check and eight focused cases passed. The new Linux run is [34571751423](https://github.com/CryogenicPlanet/artifactory/actions/runs/34571751423).

That two-worker Linux run has finished: **371 passed, 22 failed across 393 tests and 103 files**. All three serial diagnostics and all six extended source-crash cases pass. Remaining failures comprise twelve five-second whole-test timeouts, seven readiness/state polls, two 35-second scenarios, and one public-page 503-versus-401 response. The two generation-revert failures already wait 15 seconds and need lifecycle evidence; do not assume another timeout increase fixes them. The public-page failure needs the pending production policy replacement, not retries. Full log: `/tmp/comms-linux-run-34571751423.log`.

Linux CI was actually run at `181939b`: [run 34567582863](https://github.com/CryogenicPlanet/artifactory/actions/runs/34567582863). Install/check/build passed; tests produced **306 passed, 101 failed**. Beyond known local failures, representative tests exhausted one-second startup polling or a five-second whole-test budget. Logs lack child diagnostics at timeout, so neither resource pressure nor lifecycle failure is established. Reproduce representative cases serially with redacted process/status diagnostics before changing deadlines. OS ownership, container execution, descendant closure and real reboot acceptance remain unverified.

Do not repeat the public-page retry workaround. Fix the production read path and remove the retry allowance when its replacement lands.

The delta recovery fixes are committed in `a205ce1` (maintenance finalization; root check and 10 tests passed) and `e13b3af` (page-intent diagnostic access; root check and 13 tests passed). Linux diagnostics are committed in `0813fe6`; the three selected root cases passed without relaxed deadlines.

Lifecycle items 12/13 are integrated. The full root suite with two workers passed **393/393 tests across 103 files** in 282 seconds; root check/build also pass. This includes the boot reductions and both delta recovery fixes. The UI handoff is frozen, with 620 CSS lines removed and no added dependencies/tests; it includes minimal API client changes and will land with that API. Shared mutation/type/API/extension/wait integration is progressing separately; do not mark it shipped from individual worker checks. A fresh integration review found that editable migrations could leave stale public grants; complete atomic activation replacement is required before this wave ships. CSP/assets, the keeper-backed SQL hang watchdog, and 30-day receipt/outbox retention are also being composed into that wave.

The combined boot v2, final app and UI handoffs are integrated locally: **207 paths**, with every before/after hash verified and no overlapping ownership. Locked install, check and build pass. The app worker's 200/203 broad run had three failures subsequently resolved by focused recovery and generation tests. Cold dependency preparation was measured at over 15 seconds before child launch; only the two cold-manifest tests now allow 60 seconds for initial readiness, retaining their existing whole-test budgets and the default 15-second readiness elsewhere. Production durability/deadlines are unchanged.

The combined root suite finished **449 passed, 3 failed across 452 tests and 120 files** in 358 seconds (`/tmp/comms-root-combined-tests.log`). The generations fixture lacked the new watchdog ping endpoint; its one-line correction passes on root (one focused test). The two cutover-deadline tests still instrumented the old body-parser file and the wrong initializer after the refactor; both corrected tests pass on root in 24 seconds. These changes preserve production deadlines and add assertions that their instrumentation applies. A fresh integration review found reference lookups and filtered UI searches advancing read marks; both now send `mark=0`. Actual-backend browser acceptance passed board, post/read/search, draft retention through data refresh/search, topic navigation and extensions. This is not a new UI test suite.

Graceful shutdown is corrected: boot owns an intrinsic cleanup scope, closes public admission while preserving admitted publication, drains requests, closes event waiters and records keeper-backed owner retirement before listener teardown. The actual built launcher exited naturally in 69 ms versus 35–37 seconds previously. Deterministic scope regression, admitted-write/restart proof and fresh review pass. The final exact-root suite is **454/454 tests across 121 files**, 322.90 seconds, with check/build also passing. Logs: `/tmp/comms-root-shutdown-combined-tests.log`, `/tmp/comms-root-shutdown-check.log`, `/tmp/comms-root-shutdown-build.log`. This is local acceptance of the combined API/boot/UI review wave; Linux acceptance of the new commit still remains.

Commit `09f6825` contains the preceding accepted API/boot/UI/shutdown wave and is pushed to draft PR #1. Its exact-head Linux run [34576526793](https://github.com/CryogenicPlanet/artifactory/actions/runs/34576526793) finished with **437 passes and 17 failures**, after check/build and serial diagnostics passed. Failures comprise nine five-second aggregate test timeouts, five readiness assertions, a Linux process-argument limit in the oversized public-paths fixture, a token-replay readiness race, and one 35-second migration rehearsal timeout. A separate isolated lane is correcting measured fixture issues; the migration timeout remains under investigation. No production deadline has been relaxed. Full log: `/tmp/comms-linux-run-34576526793.log`.

The next wave is applied locally from `/tmp/comms-next-wave-final-handoff`: all 115 before/after hashes matched, root check/build pass and a fresh static seam review has no concrete findings. It includes app-owned topic moves, guarded SQL writes and transaction poisoning, app-owned hourly backups, manual copies, 5% sampled headroom, 20% catalogue backup budget, protected generation pruning and bounded active event-page maintenance.

Whole-root acceptance produced **498 passes and 17 failures across 515 tests/133 files**, 382.87 seconds (`/tmp/comms-next-wave-root-tests.log`). Every failure was diagnosed: eight subprocess fixtures omitted the new healthy-state setup or retained a v8-only table while reconstructing an old schema; two other downgrade tests had the same table/version mismatch; four recovery tests demanded the intentionally removed boot admission behavior from ordinary app moves; one fault test opened the deleted coordinator; one SQL test compared unrelated asynchronous extension receipts; and whole-generation restore exceeded its aggregate 60-second test budget. Production was not changed to satisfy these obsolete fixtures.

Reviewed corrections preserve rollback, publication, historical migration, namespace and restore-gate assertions. Pending-owner tests now exercise the retained manual backup guard; only the deleted coordinator-specific fault test was removed. Root verification passes **27/27 tests across nine affected files** (`/tmp/comms-next-wave-fixtures-root.log`), and the generation-restore case passes separately in **49.36 seconds** even with its original 60-second budget (`/tmp/comms-next-wave-generation-budget-root.log`). That multi-generation fixture now has a 180-second aggregate allowance for the upcoming measured fresh-preparation workflow; readiness and production deadlines are unchanged. Final check passes (`/tmp/comms-next-wave-final-check.log`). All **514 retained tests have passing evidence across these runs**; do not describe this as a single all-green 514-test run.

Raw SQL callers own domain/projection maintenance; legitimate editable migrations instead rebuild the complete public-grant snapshot during activation. Successful idempotency receipts have the owner's 30-day replay guarantee; after expiry, key reuse may execute again.

Topic move (30 paths, 59 focused tests), SQL (21 paths, 36 focused tests), and backup scheduling/copy (33 paths, 37 focused tests) now have frozen reviewed handoffs; an isolated next-wave integrator is composing them with storage. These worker counts are not combined acceptance. The SQL wave also retires a child after transaction-finalizer uncertainty, preserving reservations and keeper proof; combined SQL protections must include the new topic-page continuation table. Physical event retention, app-owned SSE and preparation-cache simplification have separate bounded workers. No strict physical-file event quota is claimed from SQLite DELETE: reclaimed B-tree pages remain reusable inside the shared database file.

## Historical parallel work and integration boundaries

All writers use isolated copies. The parent alone integrates frozen before/after manifests into this checkout and preserves owner-owned spec/review files.

Current lanes: public-API core correction and app/combined-restore integration; page-policy boundary removal; restore request queuing; boot error/event/restart integration; source-only seed reset; and restoration of the digest example. The latest full root acceptance is recorded above. The table below records the earlier wave's ownership boundaries, not a claim that those workers are all still active.

| Lane | Scope / dependency |
| --- | --- |
| Minimal API | Items 1/4/22 and retained `.handle()` adapters; SQL message filtering, comma-list mentions, exclusive cursors, removal of inbox/digest/search/read/reactions endpoints. Shares complete app schema v7 with mutation lane. |
| HTTP types | Literal code schemas, exhaustive refusal policies, defect 500s, shared wire-schema validation and SQL read adapter. Coordinates retained adapters with minimal API. |
| Shared mutations | One transaction/reservation/outbox/receipt protocol; compatible unified receipt migration; all retained writers. No partial v7 migration may be integrated. |
| Extension capabilities/core | Real extension-owned core mount, route conflict rules, public read/write/event helpers and extension migrations. Subscriptions must become an example of the public API. |
| Boot failure finalization | Delta D1/item 24: release owned request freezes on every exit while ensuring uncertain children/stores are not served. |
| Boot diagnostic access | Delta D3: committed source read/browse/history should not require unrelated page-move completion; preserve mutation and recovery guards. |
| Boot lifecycle | Items 12/13: consecutive failures and demotion events; drain-only freeze deadline with bounded backup/health steps. |
| Boot event indexes | Item 14: type/actor/instance/level projections and indexed filtering/cursors. Additive boot v14 coordinated with later public-path projection. |
| UI simplification | Item 20: agreed state ownership and Tailwind, consuming the minimal API UI changes. No new UI tests. |
| Public-page boundary | Plan complete, implementation pending lifecycle/core interfaces: exact-directory public_paths, no inherited grants, publication-atomic updates, fail-closed rebuild on database replacement, app-owned listing/write policy, coordinated CSP/assets. |

Latest delta: [review comment](https://github.com/CryogenicPlanet/artifactory/pull/1#issuecomment-5630256395), local `docs/pr-1/delta-review.md`. D1 gate leaks are confirmed. D3's overbroad diagnostic gate is confirmed, but the proposed backup-restore trigger is not established. D5's claim that conflicting intents null authentication is contradicted by the actual `Effect.exit` boundary and authenticated recovery tests; preserve fail-closed mutation rather than changing recovery order on that premise.

Current frozen handoff pointers (temporary recovery aids, not durable artifacts):

```text
/tmp/comms-fixture-triage.cjIfRL/handoff
/var/folders/2j/z4115t_51pv7sxxrq1_j7_xh0000gn/T/comms-pages-triage-bzogjcbs/handoff
/var/folders/2j/z4115t_51pv7sxxrq1_j7_xh0000gn/T/comms-recovery-diagnostics-7qhns5dc-handoff
/var/folders/2j/z4115t_51pv7sxxrq1_j7_xh0000gn/T/comms-resume-reactions-6vxfoikl/handoff
/tmp/comms-boot-reduction.udCo3F-handoff
/tmp/comms-review-first-tests.log
/tmp/comms-reduction-root-check.log
/tmp/comms-reduction-root-tests.log
/tmp/comms-linux-run-34567582863.log
```

Older SQL, automatic-mark and extension-route attempts may be selectively reused by their current owners. They are not independently mergeable against the new shared mutation/API contracts.

## Historical ranked review checklist

Numbers match the owner's review. None of these is marked complete merely because an earlier worker implemented part of it.

| # | Remaining action |
| --- | --- |
| 1 | Automatic marks on authenticated message views, highest returned message seq, `mark=0` peeking; no agent `POST /api/read`. Avoid work/events for unchanged marks; preserve lifecycle/epoch safety. |
| 2 | Top-level extension routes; reserve boot paths, `/api`, `/api/ext`; later duplicate owner fails alone; core override allowed and logged; ownership discoverable. |
| 3 | Integrated locally: reactions removed from core API/services/UI/docs; historical data retained. Unified receipt migration still belongs to item 9. |
| 4 | Remove core inbox/digest/token-budget surfaces. Add `mentions`, `exclude_self`, `newest` to messages. Preserve the recipe's home-subtree **or** mention semantics. Digest becomes `examples/extensions/digest.ts`; use “pages” consistently. |
| 5 | Make declared HttpApi schemas execute through `.handle()`; streamed routes still use the same decoders. Remove duplicated body/query parsers and schema/OpenAPI drift. |
| 6 | Literal error-code schemas and exhaustive status/hint maps. Defects become non-retriable `500 handler_failed` naming the route; preserve actionable typed errors. |
| 7 | Small extension API exposing durable message/topic/event operations, `ctx.read(effect)` and extension migrations. Move messages/topics/search/profiles into `ext/core.ts`; keep the kernel focused on the durable protocol and loading. |
| 8 | Signal-driven message and boot-event long-polls. Once streaming starts, failures must still finish with the documented envelope. Retain heartbeat, cursor and drain semantics; stop 100 ms full-query loops. |
| 9 | One `mutate({ events, idempotency?, body })` owns relay, epoch, reservation, transaction, outbox, receipt and safe abort. Every retained writer uses it. Consolidate idempotency with compatible migration and lost-response behavior. |
| 10 | Cache/pin the publication fence and consume append acknowledgements. Preserve transaction-snapshot visibility; remove repetitive fence HTTP calls from idle waits. |
| 11 | Owner selected a 30-day idempotency replay guarantee. Index unshipped outbox rows and implement safe outbox/receipt retention. Coordinate pruning with recovery evidence, reserved ranges, retries and backup restore; deleting acknowledged rows must not break replay/reconciliation. |
| 12 | Implemented in `8b3fa7a`: count consecutive startup failures, reset on healthy start, and emit one `generation.failed` on demotion. Included in the green 393-test root suite. |
| 13 | Implemented in `8b3fa7a`: drain-only budget, separate backup/go-to-health deadlines and retriable `freeze_timeout`, preserving acceptance/rollback and admitted writes. Included in the green root suite. |
| 14 | Indexed boot event columns for type/actor/instance/topic/level. Topic alone is currently projected. Remove duplicate filtering and advance empty filtered cursors correctly. |
| 15 | Bound/interrupt SQL execution and detect/retire hung children. A query must not wedge the only app thread indefinitely; preserve positive closure before replacement. |
| 16 | Emit durable lock, fs and generation transitions in their owning transactions; do not discard transition results. |
| 17 | Fix public-page availability during pending publication/reload through the reviewed boot `public_paths` projection. Do not read app policy under the long-held operation gate or paper over it with retries in tests. |
| 18 | Send the agreed CSP on agent-authored pages and verify the header at the actual served boundary. |
| 19 | Apply the review's concrete simplifications: shared parsing where still needed, exhaustive errors, one startup layer graph, shared committed-refusal/auth primitives/IPC contract, and clear admission names. Preserve independent boot recovery availability and startup ordering. No generic helper framework. |
| 20 | Use the agreed UI state model (or explicitly reconcile the documented choice), deduplicate loads, adopt Tailwind in the UI and remove redundant handwritten CSS. **No new UI tests requested.** |
| 21 | Reduce boot to its six approved jobs; relocate/delete the policies and mechanisms listed below. Add missing restart/metrics/combined restore and the required storage limits. |
| 22 | Finish the minimal API: id-or-seq `:ref`, search as message `q`, archive through topic PUT, extension-owned deletion/profiles/roster. One exclusive-since cursor/envelope contract; topic snapshot uses `fence`. Fix manifest references/boot descriptors and hash only init text for its version. Rewrite `/init`, editing docs and recipes. |
| 23 | Database portability gets a separate design/PR after base work. For now avoid gratuitous SQLite-only syntax where an actually supported portable equivalent exists, and prevent boot from learning app table names. Do not start a DbOps/backend framework in this PR. |
| 24 | Gate finalization implemented in `a205ce1`, with failure/recovery proof. Ordinary topic-move freezes remain until the separate app-owned move reduction lands. Ranked above item 17 by the delta. |

## Boot reduction: exact scope to retain when planning item 21

Keep credential-stripping proxy and auth; sequence allocation/event storage; snapshot/rehearsal/swap/rollback with positive keeper closure; edit lock/staging/publication; recovery access. Keep kernel boot-ID proof, consistent copies on demand, boot SQL, headless token minting and HTTP database restore. Keep editable dependency/UI builds; do not bake away the project's edit loop.

Remove or relocate the backup drill, boot agent roster, storage allocation walker, generation-preparation artifact/cache complexity, volume watcher, QR dependency, boot SSE, atomic page-subtree rename and topic-move coordinator. The app/extension owns hourly scheduling; boot exposes the copy mechanism at `POST /_boot/db/backup`. Simplify account listings as requested. Public-page policy projects to boot's `public_paths`; boot must not query app domain tables. Add the domain-table CI guard.

The owner explicitly accepts these changed guarantees; reconcile their older spec paragraphs instead of silently keeping contradictory promises:

- Topic-page moves become re-runnable, not atomic.
- Shell source edits no longer deploy automatically or create watcher history.
- A broken app schedule can miss hourly backups.
- App swaps disconnect app-owned SSE; boot long-poll remains available.

Keep request diagnostics, but fix the mismatch: current logging observes forwarded child dispatches; the review specifically wants boot-handled failures and enrollment/auth traffic while the child is down. Avoid feed self-logging and never record credentials.

Storage still needs 5% headroom refusal, physical event retention, the 20% backup cap and protected five-generation pruning. The review explicitly keeps headroom/event protection enforceable while the app is dead, despite moving ordinary scheduling/policy out of boot. Protect current/recovery-referenced artifacts and retry/recovery evidence; do not delete protected data just to claim a quota.

Missing retained capabilities: signed boot restart, metrics, combined generation-plus-database restore, and any still-required signed settings/reset controls. Source undo's exact HTTP-outcome replay and human source recovery through another holder's lock also need a final conformance decision; the current selection-only replay must not be described as exact outcome replay.

Owner clarification: reset app to seed replaces **source only**, preserving messages, pages and identities. This authorizes source recovery, not a data wipe. Settings still need a narrow supported contract rather than exposing the arbitrary boot settings table, which also holds recovery metadata.

## Historical execution order

1. Re-read owner decisions and current checkout; identify new review comments/deltas and preserve unrelated edits. Confirm what remains relevant among the isolated fixes. Do not automatically restart all old workers.
2. Establish the retained recovery baseline and correct its known failures. Keep a clear distinction between fixture updates and production fixes; record the actual full-suite result.
3. Follow review order with bounded parallel lanes: minimal API/removals; HttpApi/errors; shared mutation/extension capabilities. Coordinate shared schemas and `ext/core.ts` centrally; isolate writers. The shared mutation contract must settle before migrating every domain writer.
4. Then combine signal/fence/outbox work with boot correctness and reduction. UI can proceed independently once the API contracts stabilize. Enforce the accepted smaller scope instead of preserving sunk-cost code.
5. Run proportionate validation, update the PR with small coherent commits and current limitations, and reconcile the build plan/spec with the accepted behavior. No deployment or merge implied.

Use Node `22.22.3` at `/Users/cryogenicplanet/.vite-plus/js_runtime/node/22.22.3/bin/node` for local test consistency. Directory-sensitive shims selected Node 26 in temporary workers before. Local Bun was `1.4.0-canary.1+4924862cf`; CI pins published `1.4.0+34cbb9a40`. Verify current binaries, not just `bun --version`. In isolated builds, ensure `@comms/boot` resolves the isolated package: a prior acceptance run accidentally loaded root boot via symlinked workspace dependencies.

Testing remains risk-based: strong boot/auth/publication/recovery tests, focused server behavior tests, no new UI test suite, and no repeated broad test runs without a changed/failing concern. No process-global mutable state, speculative abstractions or new permanent packages merely to organize this cleanup. Never mark a slice done from a worker report alone.
