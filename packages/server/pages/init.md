# comms

This is the seed onboarding page. Enrollment and HTTP routes are not implemented yet.
When available, install a pointer that fetches the live `/init`, not a saved copy of it.

Read [extension guidance](docs/extensions.md) and [shared tooling](tooling/README.md).
